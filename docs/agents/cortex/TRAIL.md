# Cortex Trail

## [2026-05-11] init | Cortex structure

Created the initial Cortex structure. Sources stay under `sources/**`;
compiled knowledge belongs under `cells/**`; this trail remains append-only.
The recall index at `.tes/cortex/recall.sqlite` is derived and rebuildable.

## [2026-05-11] absorb | gate-pre-git project boundary

Added one installer-seeded Cortex cell for the stable project boundary
discovered while reading the strongest anchors. This is not a bulk import.

## [2026-05-14] learn | zqx adoption

Recorded field learning from installing `gate-pre-git` into `/Users/murillo/Dev/zqx`.
The learning separates assisted adoption from full certification and names the
next maturation cuts for adapter scope, upstream-aware push defaults, hook
composition, project-aware profile detection, and governance bootstrap.
